// This page is intentionally left blank as public registration is not suitable for an admin-only dashboard.
// Admin users should be created via database seeding or a protected admin interface.
export {};
